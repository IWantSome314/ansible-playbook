all:
  vars:
    ansible_user: "{{ vault_ansible_user }}"
    ansible_port: "{{ vault_ansible_port }}"
  children:
    management:
      hosts:
        slt-01:
        k3s-man-01:
    k3s:
      hosts:
        k3s-man-01:
        k3s-wrk-01:
        k3s-wrk-02:
    media:
      hosts:
        radarr:
        sonarr:
        jellyfin:
        qbit:
        jackett:
        overseer:
    monitoring:
      hosts:
        grafana:
        influxdb:
        patchmon:
    network:
      hosts:
        pihole:
        myspeed:
    dashboards:
      hosts:
        homepage:

